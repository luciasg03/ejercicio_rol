CREATE TABLE personajes (
    ID int NOT NULL,
    nombre varchar(255) NOT NULL,
    caracteristicas varchar(255),
    id_persona int,
    PRIMARY KEY (ID)
);

create table personas (
    nombre VARCHAR(255),
    id INT
);

create table